<div class="py-8">
  {% paginate blog.articles by section.settings.number_of_blog_posts %}
    <h1 class="text-center text-3xl">{{ blog.title }}</h1>
    <div class="max-w-6xl mx-auto">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
        {% for article in blog.articles %}
          <div class="w-full py-6 px-3">
            <div class="bg-white whadow-xl rounded-lg overflow-hidden">
              {% if article.image %}
                <img src="{{ article.image | img_url: 'medium' }}" alt="{{ article.image.alt | escape }}" />
              {% else %}
                {{ 'image' | placeholder_svg_tag: 'w-20' }}
              {% endif %}
              <h2>{{ article.title }}</h2>
              {% if article.excerpt.size > 0 %}
                <p>{{ article.excerpt }}</p>
              {% else %}
                <p>{{ article.content | strip_html | truncatewords: 15 }}</p>
              {% endif %}
            </div>
          </div>
        {% endfor %}
      </div>
    </div>

  {% endpaginate %}
</div>


{% schema %}
{
  "name": "Blog",
  "settings": [
    {
      "type": "range",
      "id": "number_of_blog_posts",
      "label": "Number of blog posts",
      "min": 1,
      "max": 15,
      "step": 1,
      "default": 3
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Blog",
      "category": "Blog"
    }
  ]
}
{% endschema %}